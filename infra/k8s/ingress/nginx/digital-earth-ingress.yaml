apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: digital-earth
  namespace: digital-earth
  annotations:
    # cert-manager (Ingress-shim): auto create/renew Certificate -> Secret
    cert-manager.io/cluster-issuer: letsencrypt-prod-nginx
    # HTTPS redirect
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # WAF (ModSecurity + OWASP CRS) - see: infra/k8s/waf-rules.yaml / docs/waf-configuration.md
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/enable-owasp-core-rules: "true"
    nginx.ingress.kubernetes.io/modsecurity-transaction-id: "$request_id"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - example.com
      secretName: digital-earth-tls
  rules:
    - host: example.com
      http:
        paths:
          - path: /api/v1
            pathType: Prefix
            backend:
              service:
                name: digital-earth-api
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: digital-earth-web
                port:
                  number: 80
